<p style="color: green"><%= notice %></p>

<h1>投稿一覧</h1>

<div id="microposts">
  <% @microposts.each do |micropost| %>
    <%= render micropost %>
    <p>
      <%= link_to "投稿をみる", micropost %>
    </p>
  <% end %>
</div>

<%= link_to "投稿を新規作成", new_micropost_path %>
