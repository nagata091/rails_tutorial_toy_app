<p style="color: green"><%= notice %></p>

<h1>ユーザー情報</h1>

<%= render @user %>
<p>
  <strong>投稿一覧:</strong>
  <%= @user.microposts.first.content %>
</p>

<div>
  <%= link_to "ユーザー情報を編集する", edit_user_path(@user) %> |
  <%= link_to "ユーザー一覧にもどる", users_path %>

  <%= button_to "ユーザーを削除する", @user, method: :delete %>
</div>
